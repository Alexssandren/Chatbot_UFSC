DATABASE_URL="file:./dev.db"
PORT=3000
UPLOAD_DIR=./uploads
NODE_ENV=development
SESSION_SECRET="altere-este-segredo-com-pelo-menos-32-caracteres"
CORS_ORIGIN=http://localhost:5173
# Opcional: em producao, force logs dos valores dos campos da submissao (PII)
# DEBUG_SUBMISSIONS=1

# E-mail (Fase 11) — desligado por padrao; habilite apenas com SMTP configurado
# MAIL_ENABLED=true
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM="Atividades Complementares UFSC <noreply@example.com>"
